# FILE: ``.ai/prompts/01_bootstrap_prompt.md`

# Bootstrap Prompt

Use this at the start of a new chat after attaching or pasting `.ai/LATEST_CONTEXT.md`.

```text
You are resuming a software project from a Git-backed long-term memory system.

I will provide the compiled save-state file `LATEST_CONTEXT.md`.

Your job:
1. Ingest it completely.
2. Treat `.ai/memory/*.md` as canonical and `LATEST_CONTEXT.md` as generated.
3. Produce a state synchronization report with these exact sections:
   - Synced Understanding
   - Current Objective
   - Active Tasks
   - Current Technical Model
   - Immediate Next Actions
   - Risks / Missing Information
4. Explicitly call out any contradictions, stale assumptions, or missing canonical details.
5. Do not invent progress that is not present in the provided state.
6. End with a short “ready-to-resume” summary.

Important rules:
- Prefer the roadmap and active buffer for live state.
- Prefer the global context for durable rules.
- Prefer the logic map for system reasoning.
- If the compiled file appears inconsistent, say so plainly.

Now ingest the following `LATEST_CONTEXT.md` and verify state synchronization:
[PASTE OR ATTACH .ai/LATEST_CONTEXT.md HERE]
```

---



## FILE: `.ai/prompts/02_checkpoint_prompt.md`

```md
# Checkpoint Prompt

Use this mid-session whenever a milestone has been reached and you want Git-ready updates for the canonical memory files.

```text
We are mid-session. Generate Git-ready memory updates for the repository-backed long-term memory system.

Goal:
Capture the current milestone cleanly and update only the canonical source files, not the generated artifact.

Return your answer in this exact structure:

1. Summary of Progress
2. Decisions Made
3. Changes Needed In Memory Files
4. Ready-to-Paste File Patches

For section 4, produce repo-ready updates for only the files that must change, chosen from:
- .ai/memory/01_global_context.md
- .ai/memory/02_roadmap.md
- .ai/memory/03_logic_map.md
- .ai/memory/04_active_buffer.md

Requirements:
- Keep YAML valid.
- Move any completed roadmap tasks to status: completed so the compiler can archive them.
- Keep the active buffer lean and focused on the next practical move.
- Update the logic map only if the technical mental model actually changed.
- Do not rewrite unaffected files.
- Do not output `LATEST_CONTEXT.md`; that is generated by the compiler.

Current milestone / facts to capture:
[PASTE SESSION MILESTONE NOTES HERE]
```

---


## FILE: `.ai/prompts/03_handoff_prompt.md`

```md
# Handoff Prompt

Use this at the end of a session to generate the resurrection packet and final canonical memory updates.

```text
We are ending this session. Generate a resurrection packet for the Git-backed long-term memory system.

Return your answer in this exact structure:

1. Session Outcome
2. Canonical File Updates Required
3. Ready-to-Paste Updates
4. Resume Brief For Next Session

Rules:
- Update only canonical source files in `.ai/memory/`.
- Do not output `LATEST_CONTEXT.md`.
- Keep YAML valid and compact.
- Make the next session easy to resume in under two minutes.

The `Resume Brief For Next Session` must include:
- where work stopped
- what is done
- what remains
- blockers or uncertainties
- intended first move next session
- concise rationale_summary for that first move
- files most likely to be edited next

Do not provide private internal chain-of-thought.
Do provide an explicit, concise next-step rationale that another session can act on immediately.

Session facts to hand off:
[PASTE FINAL SESSION NOTES HERE]
```

---